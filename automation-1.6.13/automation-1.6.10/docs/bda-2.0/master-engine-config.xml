<engine version="">
	<container-types>
		<container-type name="custom"/>
		<container-type name="database"/>
		<container-type name="jboss"/>
		<container-type name="tomcat"/>
	</container-types>
	<builders>
		<!-- ant, groovy, maven, ect.. Probably only start with ant -->
		<builder name="" class="" target=""/>
	</builders>
	<property-types>
		<!-- Allows us to define certain types used by all builds, we will have our own validation classs or target. These will be our custom types in addition to a base type of number, string boolean or list.
		email
		hostname
		ip-address
		url
		-->

		<property-type name="" base-type="number|string|boolean|list" validation-class="" validation-target="">
			<!-- if you want to provide a list of values add below -->
			<list-type value="single|multiple"/>
			<property-item value=""/>       
		</property-type>        
	</property-types>
	<plugin-groups>
		<!-- see sample for explaination -->
	</plugin-groups>
	<project>
	</project>
	<build-plugins>
	</build-plugin>
	<install-plugins>
	</install-plugins>
</engine>


