<?xml version="1.0" encoding="utf-8" ?>
<!--
$Id: install.xml 4757 2008-05-15 20:43:31Z zengje $
$HeadURL: http://gforge.nci.nih.gov/svnroot/generic-webapp/trunk/software/install.xml $
-->
<project name="generic-api-generic-webapp-installer" default="upgrade" basedir="." 
	xmlns:ivy="antlib:org.apache.ivy.ant"
	>
	<description>
		TBD
	</description>

	<!-- Properties file related properties and tasks -->
	<property environment="env" />
	<property file="project.properties"/>
	<property name="properties.file" value="${basedir}/install.properties"/>
	<replaceregexp file="${properties.file}" byline="true" match="^([\w\d.]+)=(.*[\w\d\/\{\}\\]+)[ \t]+\r*$" replace="\1=\2"/>
	<replaceregexp file="${properties.file}" byline="true" match="^([\w\d.]+)=[ \t]+(.*[\w\d\/\{\}\\]+)\r*$" replace="\1=\2"/>
	<property file="${properties.file}" />
	
	<!-- Generic properties -->
	<property name="lib.dir" value="${basedir}/lib" />
	<property name="working.dir" value="${basedir}/working" />
	<property name="temp.dir" value="${working.dir}/temp" />


	<!-- Install Time properties -->
	<!-- Source and target directories -->
	<property name="bda-utils.dir.src" value="${bda-utils.dir}" />
	<property name="tools.dir.src" value="${basedir}/${tools.dist.relative.dir}" />
	<property name="common.dir.src" value="${basedir}/${common.dist.relative.dir}" />
	<property name="grid.resource.dir.src" value="${working.dir}/${grid.dist.relative.dir}" />
	<property name="grid.application.dir.src" value="${working.dir}/grid-application" />
	<property name="common.dir.target" value="${working.dir}/${common.dist.relative.dir}" />
	<property name="db-install.target.dir" value="${working.dir}/${db-install.dist.relative.dir}" />
	<property name="db-upgrade.dir.target" value="${working.dir}/${db-upgrade.dist.relative.dir}" />
	<property name="jboss-conf.dir.target" value="${working.dir}/${jboss-conf.dist.relative.dir}" />

	<!-- *-ds.xml and WAR -->
	<property name="generic-webapp.dir.dist" value="${basedir}/${generic-webapp.dist.relative.dir}" />
	<property name="generic-webapp.ds.file" value="generic-webapp-ds.xml" />
	<property name="generic-webapp.app-sec-conf.file" value="ApplicationSecurityConfig.xml" />
	<property name="generic-webapp.hibernate.file" value="hibernate.cfg.xml" />
	<property name="generic-webapp.war.file" value="generic-webapp.war" />
	<property name="grid.artifact.file" value="wsrf.war.zip" />
	<property name="tomcat.grid.dir.src" value="${basedir}/${tomcat.install.dir}" />
	<property name="grid.dir.target" value="wsrf.war" />
	<property name="grid.application.name" value="cagrid_APPNANE" />


	<!-- Paths -->
	<path id="bda-utils.classpath">
		<fileset dir="${bda-utils.dir}">
			<include name="**/*.jar" />
		</fileset>
	</path>
	<!-- Task definitions -->
	<taskdef uri="antlib:org.apache.ant.antunit" resource="org/apache/ant/antunit/antlib.xml">
		<classpath>
			<pathelement location="${bda-utils.dir}/antunit-1.0.jar" />
		</classpath>
	</taskdef>
	<taskdef name="xmlconfig" classname="com.xmlconfig.anttask.XmlConfig" classpathref="bda-utils.classpath" />
	<taskdef name="for" classname="net.sf.antcontrib.logic.ForTask">
		<classpath>
			<pathelement location="${bda-utils.dir}/ant-contrib-1.0b3.jar"/>
		</classpath>
	</taskdef>
	<taskdef resource="net/sf/antcontrib/antcontrib.properties">
		<classpath>
			<pathelement location="${bda-utils.dir}/ant-contrib-1.0b3.jar"/>
			<pathelement location="${bda-utils.dir}/bcel-5.1.jar"/>
			<pathelement location="${bda-utils.dir}/commons-httpclient-3.0.1.jar"/>
			<pathelement location="${bda-utils.dir}/commons-logging-1.0.4.jar"/>
			<pathelement location="${bda-utils.dir}/ivy-1.3.1.jar"/>
		</classpath>
	</taskdef>

	<!-- Conditionals -->
	<available file="${jboss.home}/server/${jboss.server.name}/conf/jboss-service.xml" property="jboss.exists"/>
	<!-- generic-webapp can use either Oracle or MySQL or PostgreSQL as its database platform, this is controled by the database.type property.  Based on the value of this property sent several variables for use during install -->
	<switch value="${database.type}">
		<case value="oracle">
			<property name="database.dialect" value="org.hibernate.dialect.OracleDialect"/>
			<property name="is.oracle" value="true"/>
			<property name="database.driver.file" value="${bda-utils.dir}/ojdbc14-10.2.0.3.0.jar"/>
			<property name="database.driver" value="oracle.jdbc.driver.OracleDriver"/>
			<property name="db-upgrade.list.file" value="${db-upgrade.oracle.list.file}"/>
			<property name="db-upgrade.conf.file" value="${db-upgrade.oracle.conf.file}"/>
		</case>
		<case value="mysql">
			<property name="database.dialect" value="org.hibernate.dialect.MySQLDialect"/>
			<property name="is.mysql" value="true"/>
			<property name="database.driver.file" value="${bda-utils.dir}/mysql-connector-java-5.0.5.jar"/>
			<property name="database.driver" value="com.mysql.jdbc.Driver"/>
			<property name="db-upgrade.list.file" value="${db-upgrade.mysql.list.file}"/>
			<property name="db-upgrade.conf.file" value="${db-upgrade.mysql.conf.file}"/>
		</case>
		<case value="postgresql">
			<property name="database.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
			<property name="is.postgresql" value="true"/>
			<property name="database.driver.file" value="${bda-utils.dir}/postgresql-jdbc3-8.3-603.jar"/>
			<property name="database.driver" value="org.postgresql.Driver"/>
			<property name="db-upgrade.list.file" value="${db-upgrade.postgresql.list.file}"/>
			<property name="db-upgrade.conf.file" value="${db-upgrade.postgresql.conf.file}"/>
		</case>
		<default>
			<fail message="Invalid database type ${database.type}"/>
		</default>
	</switch>

	<!-- figure out whether to use install-properties.template or upgrade-proprties.template based on the name of the properties file -->
	<propertyregex property="properties.file.type"
		input="${properties.file}"
		regexp=".*(install|upgrade).*"
		select="\1"
		/>
	<echo message="Properties file type = ${properties.file.type}"/>
	<switch value="${properties.file.type}">
		<case value="install">
			<property name="properties.template.file" value="install-properties.template" />
		</case>
		<case value="install">
			<property name="properties.template.file" value="upgrade-properties.template" />
		</case>
		<default>
			<fail message="Property file name must include 'install' or 'upgrade' so it can be determined which properties template should be used. If you are not certain include 'upgrade' in the name of your proeprties file."/>
		</default>
	</switch>

	<!-- Includes-->
	<import file="${bda-utils.dir}/bda-build-utils-${bda.version}.xml" />

	<!-- Generated and used by database incremental build process, copied deploy:*:prep of build.xml -->
	<import file="db-integrate.xml" />

	<!-- Read db configs from existing ds.xml if it exists for use in upgrade installs, if already set in properties file values from properties file will be used -->
	<available file="${jboss.home}/server/${jboss.server.name}/deploy/${generic-webapp.ds.file}" property="ds.exists"/>
	<if>
		<isset property="ds.exists"/>
		<then>
			<jboss-read-dbconfig
				jboss.ds-xml.file="${generic-webapp.ds.file}"
				/>
		</then>
		<else>
			<echo message="Warning- could not find ${jboss.home}/server/${jboss.server.name}/deploy/${generic-webapp.ds.file}"/>
		</else>
	</if>

	<!-- There is any issue with copying files with a filtersfile, any properties with a value of
		another property do not get expanded (xx=${yy} @xx@ will be replaced with ${yy} not the
		value).  I have defined a filter set below for these properties, I then two two copies
		one to the resource.dir.temp using the filterset and then one to the desired directory 
		with the filter file.  If you add new properties that refer to other properties in the
		properties file please add them to the filterset-pre below.
	     Also you should include properties that are declared in this file, like database info below.
	-->
	<property name="resource.dir.temp" value="${working.dir}/tmp" />
	<filterset id="filterset.pre">
		<filter token="application.base.path" value="${application.base.path}"/>
		<filter token="application.url" value="${application.url}"/>
		<filter token="database.url" value="${database.url}"/>
		<filter token="database.system.url" value="${database.system.url}"/>
		<filter token="jboss.home" value="${jboss.home}"/>
		<!-- added internal properties that may be used in a filtered copy -->
		<filter token="database.driver" value="${database.driver}"/>
		<filter token="database.dialect" value="${database.dialect}"/>
		<filter token="hibernate.cfg.file.path" value="${hibernate.cfg.file.path}"/>
	</filterset>
	

	<!-- Start logging --> 
	<mkdir dir="${log.dir}" />
	<tstamp>
		<format property="install.time" pattern="yyyy-MM-dd-HH-mm" />
	</tstamp>
	<record name="${log.dir}/install-${install.time}.log" action="start"/>


	<!-- Installer section -->
	<target name="diag">
		<echoproperties/>
	</target>

	<!-- Clean up destination directory -->
	<target name="install:clean" description="Removes all files from the local filesystem">
		<sleep seconds="5" />
		<delete dir="${application.base.path}" quiet="false" />
	</target>

	<!-- Does directory management and copy some files with filtering to ensure token expansion -->
	<target name="install:init" description="Does directory management to initialize install">
		<mkdir dir="${working.dir}" />
		<delete dir="${working.dir}"/>
		<mkdir dir="${working.dir}" />
		<mkdir dir="${resource.dir.temp}"/>
		<!-- Copy files to ensure values containing variables are expanded, such properties are stored in filterset.pre and then copy with filter files -->
		<copy todir="${resource.dir.temp}" filtering="true">
			<fileset dir="${common.dir.src}">
				<include name="**/*"/>
			</fileset>
			<filterset refid="filterset.pre"/>
		</copy> 
		<copy todir="${common.dir.target}" filtering="true">
			<fileset dir="${resource.dir.temp}">
				<include name="**/*"/>
			</fileset>
			<filterset>
				<filtersfile file="${properties.file}"/>
				<filtersfile file="project.properties"/>
			</filterset>
		</copy> 
	</target>

	<!-- Wrapper install database target, depends on both MySQL and Oracle sub-targets, ifs should ensure only one is run.  Target path can be skipped by exlude.database also calls upgrade:database -->
	<target name="install:database" description="Re-creates database and users from baseline and applys incremental database changes." unless="exclude.database"
		depends="
		install:init
		">
		<switch value="${database.type}">
			<case value="oracle">
				<for list="${db.install.oracle.file.list}" param="db.install.oracle.file">
					<sequential>
						<run-sql-script database.url="${database.url}"
							database.user="${database.user}" 
							database.password="${database.password}" 
							sql.delimiter="/" 
							sql.delimitertype="row"
							sql.file="${db-install.target.dir}/${database.type}/@{db.install.oracle.file}"
							onerror="continue"/>
					</sequential>
				</for>
			</case>
			<case value="mysql">
				<database-create/>
				<for list="${db.install.mysql.file.list}" param="db.install.mysql.file">
					<sequential>
						<run-sql-script
							database.url="${database.url}"
							database.user="${database.user}"
							database.password="${database.password}"
							sql.file="${db-install.target.dir}/${database.type}/@{db.install.mysql.file}"
							/>
					</sequential>
				</for>
			</case>
			<case value="postgresql">
				<for list="${db.install.postgresql.file.list}" param="db.install.postgresql.file">
					<sequential>
						<run-sql-script
							database.url="${database.url}"
							database.user="${database.user}"
							database.password="${database.password}"
							sql.delimiter="/" 
							sql.delimitertype="row"
							sql.file="${db-install.target.dir}/${database.type}/@{db.install.postgresql.file}"
							/>
					</sequential>
				</for>
			</case>
		</switch>
		<antcall target="upgrade:database" inheritAll="false"/>
	</target>

	<target name="install:jboss:binaries" description="Unzip JBoss binary" unless="exclude.jboss">
		<unzip dest="${application.base.path}" src="${tools.dir.src}/${jboss.binaries.file}" />
		<if>    
			<os family="unix"/>
			<then>  
				<chmod dir="${jboss.home}/bin" perm="ugo+rx" 
					includes="**/*.sh"/>
			</then>
		</if>
	</target>

	<target name="install:jboss:generic-webapp" description="Deploy generic-webapp generic-webapp.ear, generic-webapp-api.war and common libraries" unless="exclude.jboss">
		<!-- added to ensure working.dirs are fresh for our install -->
		<delete dir="${jboss.home}/server/${jboss.server.name}/tmp"/>
		<delete dir="${jboss.home}/server/${jboss.server.name}/work"/>

		<copy file="${generic-webapp.dir.dist}/${generic-webapp.war.file}" todir="${jboss.home}/server/${jboss.server.name}/deploy" overwrite="true" />
		<copy file="${bda-utils.dir}/clm-3.2.jar" todir="${jboss.home}/server/${jboss.server.name}/lib" overwrite="true" />
		<copy file="${database.driver.file}" todir="${jboss.home}/server/${jboss.server.name}/lib" overwrite="true" />
		
		<!-- Replace this with file from ivy when project is ivy'fied -->
	</target>

	<target name="install:jboss:generic-webapp:configure" description="Configure generic-webapp application" unless="exclude.jboss">
		<copy file="${jboss-conf.dir.target}/${generic-webapp.ds.file}" todir="${jboss.home}/server/${jboss.server.name}/deploy" overwrite="true" />
		<copy file="${jboss-conf.dir.target}/${generic-webapp.app-sec-conf.file}" todir="${jboss.home}/server/${jboss.server.name}/deploy" overwrite="true" />
		<copy file="${jboss-conf.dir.target}/${generic-webapp.hibernate.file}" todir="${jboss.home}/server/${jboss.server.name}/deploy" overwrite="true" />
	</target>

	<!-- Wrapper target to configure jboss container, not deployed application -->
	<target name="install:jboss:configure" description="Configure generic-webapp" unless="exclude.jboss"
		depends="install:jboss:binaries,
		-install:jboss:configure:ports,
		-install:jboss:configure:login-conf,
		-install:jboss:configure:log4j,
		-install:jboss:configure:update-shutdown
		"/>

	<!-- Configure ports used by JBOSS (either bindings or editing jboss config.files) -->
	<target name="-install:jboss:configure:ports" description="Configure generic-webapp" unless="exclude.jboss">
		<jboss-bindings
			jboss.server.bindingfile.location="${jboss.home}/server/${jboss.server.name}/conf/bindings.xml"
			/>
	</target>

	<!-- Add custom login-config.xml content from snippet from generic-webapp/software/resources/deploy/login-config.${auth.type}-block.xml -->
	<target name="-install:jboss:configure:login-conf" unless="exclude.jboss">
		<if>    
			<equals arg1="${authentication.type}" arg2="ldap"/>
			<then>  
				<property name="login-config.block.file" value="${jboss-conf.dir.target}/login-config.ldap-block.xml"/>
			</then> 
			<else>  
				<if>    
					<equals arg1="${authentication.type}" arg2="db"/>
					<then>  
						<property name="login-config.block.file" value="${jboss-conf.dir.target}/login-config.db-block.xml"/>
					</then> 
					<else>  
						<fail message="${authentication.type} is not a valid authentiation type, please change to db or ldap and re-run the build."/>
					</else> 
				</if>   
			</else> 
		</if>   
		<echo message="Inserting ${login-config.block.file} into ${jboss.home}/server/${jboss.server.name}/conf/login-config.xml"/>
		<xmlconfig in="${jboss.home}/server/${jboss.server.name}/conf/login-config.xml" verbose="false">
			<insertbranch path="//policy" srcxmlfile="${login-config.block.file}" srcpath="//application-policy" />
		</xmlconfig>
	</target>

	<!-- Configure MQ settings based on contents of ncia/software/resrouces/deploy/jbossmq-destinations-service.block.xml -->
	<target name="-install:jboss:configure:jbossmq-dest" unless="exclude.jboss">
		<property name="jboss-mq.block.file" value="${resource.dir.target}/jbossmq-destinations-service.block.xml"/>
		<xmlconfig in="${jboss.home}/server/${jboss.server.name}/deploy/jms/jbossmq-destinations-service.xml" verbose="false">
			<insertbranch path="//server" srcxmlfile="${jboss-mq.block.file}" srcpath="//mbean[1]" />
			<insertbranch path="//server" srcxmlfile="${jboss-mq.block.file}" srcpath="//mbean[2]" />
		</xmlconfig>
	</target>


	<!-- Configure log4j settings based on contents of generic-webapp/software/resrouces/deploy/log4j.block.xml -->
	<target name="-install:jboss:configure:log4j" unless="exclude.jboss">
        <!--
		<property name="log4j.block.file" value="${resource.dir.target}/log4j.block.xml"/>
		<loadfile 
			property="xml.content"
			srcFile="${log4j.block.file}"/>

		<replaceregexp file="${jboss.home}/server/default/conf/log4j.xml" byline="true"
			match="(^.*\/log4j:configuration.)"
			replace="${xml.content}${line.separator}\1"/>
		-->
	</target>

	<!-- Calls to bda macros for basic functionality -->
	<target name="install:jboss:stop" if="jboss.exists" unless="exclude.jboss">
		<jboss-stop-jboss jboss.server.jndi.port="${jboss.server.jndi.port}"/>
		<jboss-stop-jboss />
	</target>
	<target name="install:jboss:start"  unless="exclude.jboss">
		<if>
			<not>
				<isset property="exclude.start.servers"/>
			</not>
			<then>
				<jboss-start-jboss />
			</then>
		</if>
	</target>
	<target name="install:validation:pre-install">
		<validate-pre-install />
	</target>
	<target name="install:validation:pre-install:ports">
		<validate-ports-preinstall />
	</target>
	<target name="install:validation:post-install">
		<if>
			<not>
				<isset property="exclude.start.servers"/>
			</not>
			<then>
				<validate-post-install />
			</then>
		</if>
	</target>
	<target name="-install:jboss:configure:update-shutdown" unless="exclude.jboss">
		<jboss-update-shutdown />
	</target>

	<!-- Wrapper target to deploy JBOSS container -->
	<target name="install:jboss" unless="exclude.jboss"
		depends="
		install:validation:pre-install,
		install:jboss:stop,
		install:validation:pre-install:ports,
		install:clean,
		install:init,
		install:jboss:binaries,
		install:jboss:generic-webapp:re-configure,
		install:jboss:generic-webapp,
		install:jboss:generic-webapp:configure,
		install:jboss:configure,
		install:jboss:start
		" />

	<!-- Wrapper target to deploy all containers to one server -->
	<target name="install" description="Installs and configures JBOSS, creates database, and deploys application" 
		depends="
		install:validation:pre-install,
		install:jboss:stop,
		install:validation:pre-install:ports,
		install:clean,
		install:init,
		install:database,
		install:jboss,
		install:jboss:start,
		install:validation:post-install
		"/>

	<!-- Fixes hibernate dialect and other configurations in code generated at build time.  This is requred because the properties/options selected at build time can be different than those used at install time. -->
	<target name="install:jboss:generic-webapp:re-configure" unless="exclude.jboss">
	    
		<delete dir="${working.dir}/generic-webapp"/>
		<unzip src="${generic-webapp.dir.dist}/${generic-webapp.war.file}" dest="${working.dir}/generic-webapp"/>
		<copy todir="${working.dir}/generic-webapp/WEB-INF/classes" file="${jboss-conf.dir.target}/hibernate.cfg.xml"/>

		<move file="${generic-webapp.dir.dist}/${generic-webapp.war.file}" tofile="${generic-webapp.dir.dist}/${generic-webapp.war.file}.orig"/>
		<war destfile="${generic-webapp.dir.dist}/${generic-webapp.war.file}" compress="false" webxml="${working.dir}/generic-webapp/WEB-INF/web.xml" >
			<fileset dir="${working.dir}/generic-webapp">
				<include name="*/**" />
			</fileset>
		</war>  

	</target>

	<!-- tomcat targets -->
	<target name="install:tomcat:binaries" description="Install tomcat for " unless="exclude.tomcat">
		<delete dir="${tomcat.base.path}"/>
		<unzip dest="${tomcat.base.path}" src="${tools.dir.src}/${tomcat.binaries.file}" />
		<if>    
			<os family="unix"/>
			<then>  
				<chmod dir="${tomcat.home}/bin" perm="ugo+rx" 
					includes="**/*.sh"/>
			</then>
		</if>
	</target>

	<target name="install:tomcat:configure" description="Configure tomcat for use with  (change ports)" unless="exclude.tomcat">
		<xmlconfig in="${tomcat.home}/conf/server.xml" verbose="false">
			<update path="(//Connector)[@port=8080]/@port" value="${tomcat.port.http}" />
			<update path="(//Connector)[@redirectPort=8443]/@redirectPort" value="${tomcat.port.ssl}" />
			<update path="(//Connector)[@port=8009]/@port" value="${tomcat.port.ajp}" />
			<update path="(//Connector)[@port=8443]/@port" value="${tomcat.port.ssl}" />
			<update path="(//Server)[@port=8005]/@port" value="${tomcat.port.shutdown}" />
		</xmlconfig>
	</target>

	<target name="install:tomcat:stop" if="tomcat.tomcat.exists" unless="exclude.tomcat">
		<tomcat-stop tomcat.home="${tomcat.home}"/>
	</target>

	<target name="install:tomcat:start" unless="exclude.tomcat">
		<if>    
			<not>   
				<isset property="exclude.start.servers"/>
			</not>  
			<then>  
				<tomcat-start tomcat.home="${tomcat.home}"/>
			</then> 
		</if>   
	</target>


	<!-- Grid Targets for jboss and tomcat -->

	<target name="install:tomcat:grid" description="Deploy  to tomcat" unless="exclude.tomcat">
		<mkdir dir="${tomcat.home}/webapps/${.dir.target}"/>
		<unzip dest="${tomcat.home}/common/lib" src="${grid.resource.dir.src}/${resource.file.jboss-globus-lib}" />
		<unzip dest="${tomcat.home}/webapps/${grid.dir.target}" src="${grid.resource.dir.src}/${resource.file.jboss-globus-war}" />
		<unzip dest="${tomcat.home}/webapps/${grid.dir.target}" src="${grid.application.dir.src}/${grid.artifact.file}" />
	</target>

	<target name="install:tomcat:grid:configure" description="Configure tomcat-application" unless="exclude.tomcat">
		<replace dir="${tomcat.home}/webapps/${grid.dir.target}" token="localhost:8080" value="${tomcat.hostname}:${tomcat.port.http}">
			<include name="**/*.wsdl" />
		</replace>
		<replace file="${tomcat.home}/webapps/${grid.dir.target}/WEB-INF/etc/globus_resolution_service/jndi-config.xml" token="localhost:8080" value="${tomcat.hostname}:${tomcat.port.http}"/>
		<replace file="${tomcat.home}/webapps/${tomcat.grid.target}/WEB-INF/web.xml" token="8080" value="${tomcat.port.http}"/>
		<replaceregexp file="${tomcat.home}/webapps/${grid.dir.target}/WEB-INF/etc/cagrid_${grid.application.name}/jndi-config.xml" 
			match="(\s+.value.)http://.*(.\/value.)" 
			replace="\1http://${tomcat.hostname}:${tomcat.port.http}/ncia-api/http/remoteService\2" 
			flags="g"/>
		<replaceregexp file="${tomcat.home}/webapps/${grid.dir.target}/WEB-INF/etc/cagrid_${grid.application.name}/${grid.application.name}_registration.xml"
			match="(\s+.wsa:Address.).*(.\/wsa:Address.)" 
			replace="\1${grid.index.url}\2"
			flags="g"/>
	</target>

	<target name="install:jboss:grid" description="Deploy  to jboss" unless="exclude.jboss">
		<mkdir dir="${jboss.home}/webapps/${.dir.target}"/>
		<unzip dest="${jboss.home}/server/${jboss.server.name}/lib" src="${grid.resource.dir.src}/${resource.file.jboss-globus-lib}" />
		<unzip dest="${jboss.home}/server/${jboss.server.name}/deploy/${grid.dir.target}" src="${grid.resource.dir.src}/${resource.file.jboss-globus-war}" />
		<unzip dest="${jboss.home}/server/${jboss.server.name}/deploy/${grid.dir.target}" src="${grid.application.dir.src}/${grid.artifact.file}" />
	</target>

	<target name="install:jboss:grid:configure" description="Configure tomcat-application" unless="exclude.jboss">
		<replace dir="${jboss.home}/webapps/${grid.dir.target}" token="localhost:8080" value="${jboss.hostname}:${tomcat.port.http}">
			<include name="**/*.wsdl" />
		</replace>
		<replace file="${jboss.home}/webapps/${grid.dir.target}/WEB-INF/etc/globus_resolution_service/jndi-config.xml" token="localhost:8080" value="${jboss.hostname}:${jboss.port.http}"/>
		<replace file="${jboss.home}/webapps/${jboss.grid.target}/WEB-INF/web.xml" token="8080" value="${jboss.port.http}"/>
		<replaceregexp file="${jboss.home}/webapps/${grid.dir.target}/WEB-INF/etc/cagrid_${grid.application.name}/jndi-config.xml" 
			match="(\s+.value.)http://.*(.\/value.)" 
			replace="\1http://${tomcat.hostname}:${tomcat.port.http}/ncia-api/http/remoteService\2" 
			flags="g"/>
		<replaceregexp file="${tomcat.home}/webapps/${grid.dir.target}/WEB-INF/etc/cagrid_${grid.application.name}/${grid.application.name}_registration.xml"
			match="(\s+.wsa:Address.).*(.\/wsa:Address.)" 
			replace="\1${grid.index.url}\2"
			flags="g"/>
	</target>


	<!-- Wrapper target to upgrade jboss container. Does not install or configure binaries -->
	<target name="upgrade:jboss" unless="exclude.jboss"
		depends="
		install:validation:pre-install,
		install:jboss:stop,
		install:init,
		install:jboss:generic-webapp:re-configure,
		install:jboss:generic-webapp,
		install:jboss:start
		" />

	<!-- Wrapper target to upgrade all container. Does not install or configure binaries -->
	<target name="upgrade" description="Deploys application and runs incremental database build" 
		depends="
		upgrade:init,
		install:validation:pre-install,
		install:jboss:stop,
		install:init,
		upgrade:database,
		upgrade:jboss,
		install:validation:post-install
		"/>

	<!-- Wrapper upgrade database target, depends on both MySQL and Oracle sub-targets, ifs should ensure only one is run.  Target path can be skipped by exlude.database. -->
	<target name="upgrade:database" description="Apply incremental DB changes" unless="exclude.database"
		depends="
		install:init,
		upgrade:init
		">
		<!-- **figure out the changes/scripts based upon current version IN the database, if any ** -->
		<switch value="${database.type}">
			<case value="oracle">
				<if>
					<available file="${db-upgrade.list.file}"/>
					<then>
						<property name="db.continue" value="true"/>
						<database-create-control-table
							database.url="${database.url}"
							database.user="${database.user}"
							database.password="${database.password}"
							/>
						<!-- sql.delimiter=/ and sql.delimiter.type=row are required to allow jdbc to run programmatic sql like ceation of procedures or functions or triggers.  This means sql files must have all commands terminated with '/' on it's own line, not ;.	-->
						<database-identify-scripts
							sqlfiles.list.file="${db-upgrade.list.file}"
							build.properties.file="${db-upgrade.conf.file}"
							sql.delimiter="/" 
							sql.delimitertype="row"
							database.sql.dir="${db-upgrade.dir.target}"/>
					</then>
					<else>
						<echo message="Could not find an incremental file list file ${db-upgrade.list.file}, there must not be any incrmental updates."/>
					</else>
				</if>
			</case>
			<case value="mysql">
				<if>
					<available file="${db-upgrade.list.file}"/>
					<then>
						<property name="db.continue" value="true"/>
						<database-create-control-table
							database.url="${database.url}"
							database.user="${database.user}"
							database.password="${database.password}"
							/>
						<database-identify-scripts
							sqlfiles.list.file="${db-upgrade.list.file}"
							build.properties.file="${db-upgrade.conf.file}"
							database.sql.dir="${db-upgrade.dir.target}"
							/>
					</then>
					<else>
						<echo message="Could not find an incremental file list file ${db-upgrade.list.file}, there must not be any incrmental updates."/>
					</else>
				</if>
			</case>
			<case value="postgresql">
				<if>
					<available file="${db-upgrade.list.file}"/>
					<then>
						<property name="db.continue" value="true"/>
						<database-create-control-table
							database.url="${database.url}"
							database.user="${database.user}"
							database.password="${database.password}"
							/>
						<database-identify-scripts
							sqlfiles.list.file="${db-upgrade.list.file}"
							build.properties.file="${db-upgrade.conf.file}"
							sql.delimiter="/" 
							sql.delimitertype="row"
							database.sql.dir="${db-upgrade.dir.target}"/>
					</then>
					<else>
						<echo message="Could not find an incremental file list file ${db-upgrade.list.file}, there must not be any incrmental updates."/>
					</else>
				</if>
			</case>
		</switch>
		<echo  message="db.continue=${db.continue}"/>
		<if>
			<isset property="db.continue"/>
			<then>
				<!-- **make the changes to the db aka invoke the proper scripts ** -->
				<antcall target="database-integrate" />
				<!-- ** make sure the versioning information IN the database is updated to reflect what we just did ** -->
				<database-update-version-table
					database.driver="${database.driver}"
					database.url="${database.url}"
					database.user="${database.user}"
					database.password="${database.password}"/>
			</then>
		</if>
	</target>

	<target name="upgrade:init">
		<echo  message="Checking if database properties exist.  If they do not on an upgrade install then ${jboss.home}/server/${jboss.server.name}/deploy/${generic-webapp.ds.file} may not exist."/>
		<property name="db.prop.list" value="database.url,database.user,database.password,database.name"/>
		<properties-exist properties.list="${db.prop.list}"/>
	</target>
</project>
